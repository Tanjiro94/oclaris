version: "3.9"

services:
  backend:
    image: oclaris_backend_prod
    build:
      context: ./back
      dockerfile: Dockerfile
    env_file:
      - ./back/.env.prod
    ports:
      - "3000:3000"
    depends_on:
      - redis

  frontend:
    image: oclaris_frontend_prod
    build:
      context: ./front
      dockerfile: Dockerfile
    env_file:
      - ./front/.env.prod
    ports:
      - "80:80"
    depends_on:
      - backend
    volumes:
      - ./docker/nginx/default.conf:/etc/nginx/conf.d/default.conf:ro

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
